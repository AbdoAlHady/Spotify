import 'package:flutter/material.dart';

class SongPlayerBody extends StatelessWidget {
  const SongPlayerBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Song Player Body'),
    );
  }
}